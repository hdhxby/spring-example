server:
    port: 8443

spring:
    application:
        name: webflux
#    datasource:
#        schema:
management:
    endpoints:
        web:
            exposure:
                include: "*"
    endpoint:
        health:
            show-details: ALWAYS

foo:
    name: foo